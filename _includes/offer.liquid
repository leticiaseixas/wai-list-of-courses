<article class="tool" id="{{ offer.id }}">
  <h3>
    {% if offer.offer_link %}
      <a href="{{ offer.offer_link }}" rel="external">{{ offer.name }}</a>
    {% else %}
      {{ offer.name }}
    {% endif %}
     <span>by {{ offer.provider }}</span> <a href="#" class="tag">{{ offer.type_of_offer }}</a>
  </h3>
</article>
