{%- assign filteredItems = "" | split: "," -%}
{%- assign curricula = site.data.wai-curricula | map: "modules" -%}

{{ include.data }}
{{ curricula | inspect }}

{%- for modules in curricula -%}
    {%- for module in modules -%}
        {%- if include.data contains module.id -%}
            {{ module.name }}
        {%- endif -%}
    {%- endfor -%}
{%- endfor -%}