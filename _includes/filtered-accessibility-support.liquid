<!-- 
data =     
"id_support": ["caption-audio", "material-ahead-of-time", "A captioned recording with transcript will be shared with attendees after the live event"] 
-->
Data
: {{ include.data | inspect }}
{%- assign filteredItems = "" | split: "," -%}
{%- assign filteredItems = site.data.accessibility-support | map: "options" -%}


New Test
{%- for item in filteredItems -%}
    {%- for aSupport in item -%}
        {% if include.data contains aSupport.id %}
        : {{ aSupport.id }}        
        {% endif %}    
        {%- assign newItems = aSupport | map: "options" -%}
        {%- for newItems2 in newItems -%}
        {% if include.data containsnewItems2.id %}
            : {{ aSupport.id }}        
        {% endif %}    
        {%- endfor -%}            
    {%- endfor -%}        
{%- endfor -%}





