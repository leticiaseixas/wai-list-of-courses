<!-- 
data =     
"id_support": ["caption-audio", "material-ahead-of-time", "A captioned recording with transcript will be shared with attendees after the live event"] 
-->
:Data
{{ include.data | inspect }}
{%- assign filteredItems = "" | split: "," -%}


{% assign aSupports = site.data.accessibility-support | map:"options" %}

{%- for aSupport in aSupports -%}
    {% assign newItem = aSupport | map: "options" -%}
    {% assign filteredItems = filteredItems | push: newItem -%}
{%- endfor -%}

: Filteres Items
{{ filteredItems | inspect }}